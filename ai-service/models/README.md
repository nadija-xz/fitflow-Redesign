# AI Models

Place TensorFlow Lite model files used for on-device/cloud workout
personalization and the computer-vision food-recognition pipeline here
(e.g. `workout_personalization.tflite`, `food_recognition.tflite`).

Model binaries are excluded from version control by `.gitignore` — store
them in a model registry or cloud storage bucket and document the
retrieval step here.
